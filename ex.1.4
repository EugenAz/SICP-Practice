(define (a-plus-abs-b a b) ((if (> b 0) + -) a b))

#| Определяется процедура, в которой, проверяя b на положительность,
   определяем, нужно ссумировть a и b (в случае положительного b) либо вычесть b из a(в обратном случае). |#
