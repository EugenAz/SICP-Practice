(define (p) (p))
(define (test x y)
  (if (= x 0) 0 y))

(test 0 (p))

#| Если интерпретатор использует нормальный порядок вычислений, то на выходе получим 0, т.к. рекурсивная функция не вызовется. Если используется аппликативный порядок, то при редуцировании аргументов процедуры вызовется функция p, что создаст бесконечную рекурсию. |#
